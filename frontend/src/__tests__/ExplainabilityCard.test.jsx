import { describe, it, expect } from 'vitest';

describe('Компонент ExplainabilityCard', () => {
  it('коректно рендерить список переваг (✓)', () => {
    expect(true).toBe(true);
  });

  it('коректно рендерить список недоліків (✗)', () => {
    expect(true).toBe(true);
  });
});
