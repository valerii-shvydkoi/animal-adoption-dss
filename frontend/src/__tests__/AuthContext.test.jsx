import { describe, it, expect } from 'vitest';
// Базова заглушка для тестів контексту авторизації.
// Детальне тестування потребуватиме @testing-library/react, яке ми налаштуємо пізніше.

describe('AuthContext', () => {
  it('має коректно ініціалізуватися', () => {
    expect(true).toBe(true);
  });
});
