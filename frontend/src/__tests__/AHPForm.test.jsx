import { describe, it, expect } from 'vitest';

describe('Компонент AHPForm', () => {
  it('відображає всі три групи критеріїв', () => {
    expect(true).toBe(true);
  });
  it('блокує кнопку при невалідній матриці (CR >= 0.1)', () => {
    expect(true).toBe(true);
  });
});
