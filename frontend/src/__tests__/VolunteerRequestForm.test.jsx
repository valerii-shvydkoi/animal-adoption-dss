import { describe, it, expect } from 'vitest';

describe('Компонент VolunteerRequestForm', () => {
  it('відображає статус замість форми, якщо заявка вже подана', () => {
    expect(true).toBe(true);
  });
});
